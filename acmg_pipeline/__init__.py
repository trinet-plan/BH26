"""BH26 evidence-first ACMG evaluator."""

# Carried over from acmg/__init__.py, which c5b303f folded into this package.
# automated_output.py records it as `tool_version` in every run manifest, so a
# run stays attributable to the code that produced it.
__version__ = "0.1.0"

VLLM_BASE_URL=http://113.43.212.98:8000/v1/
VLLM_API_KEY=9jet8eLLYBiCFA3RVZO2ewdWMvKpfLuR2IBgMZk6snI=
# VLLM_MODEL=google/gemma-4-26B-A4B-it   # optional override, this is already the default

# --- Optional: switch the LLM backend to Claude instead of the self-hosted
# vLLM server above (see acmg_pipeline/llm_client.py). Uncomment and fill in
# your own key - never commit a real key, and never paste one into a chat/
# agent session.
# LLM_PROVIDER=claude
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-sonnet-5   # optional override, this is already the default

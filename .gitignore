# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
pytest-cache-files-*/
.work/
work/

# OS
.DS_Store

# Office lock files
~$*

# Secrets / local config
.env

# Generated run logs
logs/*.log
.python-version

# API results written when ACMG_API_OUTPUT_DIR is set: one file per request,
# so the directory grows with traffic and holds patient-derived judgments.
api_results/

# Virtual environments
.venv/
venv/

# Test caches
.pytest_cache/
pytest-cache-files-*/

# Local scratch directories
work/
.work/

# Cached PubMed full-text (acmg_pipeline/fulltext_cache.py) - regenerable,
# and shouldn't put paper full text into git history
cache/

# Local, session-scoped PR review notes - not meant to ship in the repo
PR_REVIEW_NOTES.md
